# Optional override if your local Supabase CLI uses a rotated local public key.
# Copy to .env.preprod.local; never place production values in that file.
VITE_SUPABASE_URL=http://127.0.0.1:54321
VITE_SUPABASE_PUBLISHABLE_KEY=replace-with-the-browser-safe-key-from-supabase-status
